# 📱 React Native Expo App

This project is a React Native mobile application built using **Expo**, **Expo Router**, **React Components**, and **NativeWind** for styling.

It demonstrates a modern approach to building scalable mobile apps with file-based routing, reusable components, and utility-first styling.

---

## 🚀 Tech Stack

- **Expo** – Fast and flexible React Native development
- **Expo Router** – File-based routing system for navigation
- **React Native** – Core UI framework
- **NativeWind** – Tailwind CSS–style utility classes for React Native
- **TypeScript / JavaScript** – Application logic

---

## 📂 Project Structure

```text
app/
├── _layout.tsx        # Root layout (navigation setup)
├── index.tsx          # Home screen
├── join.tsx           # Join page
components/
├── Button.tsx         # Reusable UI components
assets/
├── images/            # App images and icons
